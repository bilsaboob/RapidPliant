<RapidView x:Class="RapidPliant.App.EarleyDebugger.Views.AppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <RapidGrid Orientation="Vertical">
        
        <ComboBox ItemsSource="{BindTo Grammars}" SelectedItem="{BindTo Grammar}" DisplayMemberPath="Name"></ComboBox>
        
        <RapidGrid Orientation="Horizontal">
            <Button Click="{BindTo StartParsing()}">Start</Button>
            <Button Click="{BindTo ResetParsing()}">Reset</Button>

            <Button Click="{BindTo ParseRunner.ScanNext()}">Scan next</Button>
            <Button Click="{BindTo ParseRunner.LexNext()}">Lex next</Button>
        </RapidGrid>
    </RapidGrid>
</RapidView>
